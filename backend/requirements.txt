# CrisisIQ backend — Python 3.11–3.14 (async DB via psycopg3, not asyncpg).

# --- Web API ---
fastapi>=0.124.1,<1.0.0
uvicorn>=0.34.0,<1.0.0
httpx>=0.28.0,<1.0.0
python-multipart>=0.0.20,<1.0.0
python-dotenv>=1.0.1,<2.0.0
pydantic>=2.10.3,<3.0.0
pydantic-settings>=2.6.1,<3.0.0

# --- Database (psycopg3: sync + async, pre-built wheels on Windows) ---
sqlalchemy[asyncio]==2.0.36
psycopg[binary]>=3.2,<4
alembic==1.14.0

# --- LLM (existing agents) ---
google-generativeai==0.8.3
google-auth>=2.29.0,<3.0.0
requests>=2.32.0,<3.0.0

# Full Google ADK orchestration is optional. Vercel's 500 MB Lambda limit is
# too small for google-adk and its transitive dependencies; the production
# CRISISIQ_ADK_MODE=direct path does not require it.
